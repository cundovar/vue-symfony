events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        server_name localhost;

        root /var/www/symfony/public;
        index index.php;

        # Resolver Docker pour résolution DNS dynamique
        resolver 127.0.0.11 valid=30s ipv6=off;

        # 🧱 Fichiers statiques Symfony et Vite (build ou proxy)
        location ~ ^/(bundles|css|js|images|fonts)/ {
            try_files $uri $uri/ =404;
            access_log off;
            expires 1y;
            add_header Cache-Control "public";
        }

        # 🔁 Assets Vite : fichiers statiques en prod, proxy vers Vite en dev
        location /build/ {
            # Essaie d'abord de servir le fichier statique (prod)
            # Si non trouve, proxy vers Vite (dev)
            try_files $uri @vite_dev;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        location /build/@vite/ {
            set $vite_upstream http://front:5173;
            proxy_pass $vite_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        location @vite_dev {
            set $vite_upstream http://front:5173;
            proxy_pass $vite_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # 🔁 Proxy WebSocket Vite (nécessaire pour HMR)
        location /@vite/ {
            set $vite_upstream http://front:5173;
            proxy_pass $vite_upstream/@vite/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        location /src/ {
            set $vite_upstream http://front:5173;
            proxy_pass $vite_upstream/src/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # 📱 Fichiers PWA avec headers appropriés
        location ~ ^/spa/manifest\.webmanifest$ {
            root /var/www/symfony/public;
            types { application/manifest+json webmanifest; }
            add_header Cache-Control "public, max-age=3600";
            access_log off;
            try_files $uri =404;
        }
        
        location ~ ^/spa/(sw\.js|workbox-.*\.js)$ {
            root /var/www/symfony/public;
            add_header Content-Type "application/javascript";
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            access_log off;
            try_files $uri =404;
        }
        
        location ~ ^/spa/.*\.(png|svg|ico)$ {
            root /var/www/symfony/public;
            add_header Cache-Control "public, max-age=31536000";
            access_log off;
            try_files $uri =404;
        }

        # 🤖 Proxy vers le serveur Node agents-cours (tourne sur l'hôte)
        location ^~ /api/agents-cours/ {
            rewrite ^/api/agents-cours/(.*)$ /api/$1 break;
            proxy_pass http://172.18.0.1:3000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_connect_timeout 10s;
            proxy_send_timeout 300s;
        }

        # 🎯 API backend Symfony
        location ^~ /api {
            try_files $uri /index.php$is_args$args;
        }

        # 🔄 Fallback Symfony : toutes les routes passées à index.php
        location / {
            try_files $uri /index.php$is_args$args;
        }
        # 🔁 Redirige toutes les routes SPA vers index.php pour que Vue Router fonctionne
        location /spa/ {
            try_files $uri /index.php$is_args$args;
        }
        # 🐘 Fichiers PHP traités par PHP-FPM (le container "php")
        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass php:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            fastcgi_param DOCUMENT_ROOT $realpath_root;
        }

        # 🧾 Logs
        access_log /var/log/nginx/access.log;
        error_log  /var/log/nginx/error.log warn;
    }
}
